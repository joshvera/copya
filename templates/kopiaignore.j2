# Managed by pyinfra from group_data/all.py.
# Patterns are relative to {{ backup_source }}.
{% for entry in ephemeral_exclude_patterns -%}
# {{ entry.reason }}
{{ entry.pattern }}
{% endfor %}
{% for pattern in backup_ignore_patterns -%}
{{ pattern }}
{% endfor %}
